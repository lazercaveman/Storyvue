import Vue from 'vue';
import Vuex from 'vuex';
import knobs from './modules/knobs';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    knobs,
  },
});
